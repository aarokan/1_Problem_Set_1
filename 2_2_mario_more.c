#include <cs50.h>
#include <stdio.h>

/* Recreate Mario double pyramid, albeit in text, using hashes (#) for bricks */

int main(void)
{
    /*  
    Prompt user for height
    If height is less than 1 or greater than 8 
    (or not an integer at all), go back one step 
    */

   /*  
    Iterate from 1 through height. On iteration i, print:
    (required empty space + i hashes + double space + i hashes)
    and then a newline 
    */
}